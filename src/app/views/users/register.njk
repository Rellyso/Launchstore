{% extends "layout.njk"%}

{% import "parts/cards.njk" as card %}

{% block content %}

    {% call card.render('Registre-se', 'users-register container') %}

    <form method="POST" action="/users">

        <div class="fields">
            {% include 'users/fields.njk' %}
        </div>

        <button class="button" type="submit">Salvar</button>
    </form>

    {% endcall %}

{% endblock content %}